# PIN de acceso. Cámbialo. No es Fort Knox, pero evita que entre cualquiera.
APP_PIN=cambia-esto

# Secreto para firmar el token. Genera uno con:  openssl rand -hex 32
SESSION_SECRET=cambia-esto-tambien

PORT=3000
DATA_DIR=/app/data
